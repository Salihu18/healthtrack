import 'package:flutter/material.dart';

class AppColors {
  // Background layers — deep dark like WhatsApp dark mode
  static const Color background    = Color(0xFF111B21);
  static const Color surface       = Color(0xFF1F2C34);
  static const Color card          = Color(0xFF2A3942);

  // Accent — WhatsApp-inspired teal-green
  static const Color primary       = Color(0xFF00BFA5);
  static const Color primaryLight  = Color(0xFF1DE9B6);

  // Text
  static const Color textPrimary   = Color(0xFFE9EDEF);
  static const Color textSecondary = Color(0xFF8696A0);

  // Semantic
  static const Color success       = Color(0xFF4CAF50);
  static const Color warning       = Color(0xFFFFC107);
  static const Color danger        = Color(0xFFEF5350);
  static const Color caloriePurple = Color(0xFFAB47BC);
}